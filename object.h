// collect useful structure

#ifndef OBJECT_H_
    #define OBJECT_H_
    // triangle
    typedef struct _triangle triangle;
    typedef struct _linked_triangle linked_triangle;

    // camera
    typedef struct _camera camera;

    // screen
    typedef struct _screen screen;
#endif